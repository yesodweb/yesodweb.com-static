# yesodweb.com-static

Static copy of yesodweb.com for simpler hosting

To generate these files, go to the yesodweb.com repo, go to the `make-it-static` subdirectory, and run `cargo run`. Copy the produced `public` directly into this repo.
